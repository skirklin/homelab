{
  "name": "homelab",
  "private": true,
  "version": "0.0.0",
  "description": "Personal web apps monorepo — migrating from Firebase to PocketBase + Caddy",
  "scripts": {
    "build": "turbo run build --concurrency=2",
    "dev": "turbo run dev",
    "lint": "turbo run lint --concurrency=2",
    "lint:pb": "infra/scripts/lint-pb-migrations.sh",
    "test": "turbo run test --concurrency=1",
    "test:playwright": "turbo run test:playwright --concurrency=1",
    "test:pb-hooks": "cd packages/backend && pnpm vitest run src/pocketbase-hooks",
    "test:ingest": "cd services/ingest && env -u VIRTUAL_ENV uv sync --group dev --quiet && env -u VIRTUAL_ENV uv run pytest",
    "test:env:up": "infra/test-env.sh up",
    "test:env:down": "infra/test-env.sh down",
    "test:env:status": "infra/test-env.sh status",
    "typecheck": "turbo run typecheck --concurrency=2"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@9.15.4"
}
