node_modules/
dist/
.turbo/
*.tsbuildinfo
.env
.env.local
__pycache__/
*.pyc
.venv/

# TLS certs
*.pem
*.key

handoff/

# Exported Firestore data (contains real user data)
services/scripts/export/data/

# Three Man Team working files + skills (embedded git repos)
.claude/settings.json
.claude/skills/
.claude/worktrees/

# Local snapshots of prod data pulled during dev (not for commit)
.life-log-snapshot.json
ARCHITECT-BRIEF.md
BUILD-LOG.md
REVIEW-REQUEST.md
REVIEW-FEEDBACK.md
SESSION-CHECKPOINT.md
test-results/
playwright-report/
.data/
.mcp.json
services/api/run-mcp.sh

# Per-worktree test env port (written by infra/test-env.sh up)
.test-env-port
# Per-worktree mutex used by `infra/test-env.sh fresh` to serialize concurrent
# pre-deploy gates within the same worktree
.test-env.lock
