# Daily upkeep task notification check — 8 AM Pacific
apiVersion: batch/v1
kind: CronJob
metadata:
  name: upkeep-notifications
  namespace: homelab
spec:
  schedule: "0 8 * * *"
  timeZone: "America/Los_Angeles"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: trigger
              image: curlimages/curl:latest
              command: ["sh", "-c"]
              args:
                - |
                  curl -sf -X POST \
                    -H "Authorization: Bearer $HOMELAB_API_TOKEN" \
                    http://functions.homelab.svc.cluster.local:3000/notifications/upkeep-check
              envFrom:
                - secretRef:
                    name: api-secrets
---
# Life session reminders — every minute. The function decides per-log
# whether the current minute matches the configured morning/evening
# reminder time in the owner's timezone (±1 min window).
# Idempotent via last_{morning,evening}_reminder_sent = "YYYY-MM-DD".
apiVersion: batch/v1
kind: CronJob
metadata:
  name: life-reminders-check
  namespace: homelab
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: trigger
              image: curlimages/curl:latest
              command: ["sh", "-c"]
              args:
                - |
                  curl -sf -X POST \
                    -H "Authorization: Bearer $HOMELAB_API_TOKEN" \
                    http://functions.homelab.svc.cluster.local:3000/notifications/life-reminders-check
              envFrom:
                - secretRef:
                    name: api-secrets
---
# Travel notifications — hourly tick. The function decides whether each
# active trip is at its local 7am (morning push) or 8pm (evening prompt).
# Per-trip timezone is derived from the day's first activity coords.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: travel-tick
  namespace: homelab
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: trigger
              image: curlimages/curl:latest
              command: ["sh", "-c"]
              args:
                - |
                  curl -sf -X POST \
                    -H "Authorization: Bearer $HOMELAB_API_TOKEN" \
                    http://functions.homelab.svc.cluster.local:3000/notifications/travel-tick
              envFrom:
                - secretRef:
                    name: api-secrets
---
# Daily PocketBase backup — 2 AM Pacific. Snapshots the whole pb_data
# dir (collections + records + uploaded files) into a zip in
# /pb/pb_data/backups/ on the PocketBase PVC. The 2026-05-22 life_events
# migration data loss (recovered only because a pre-migration-20260419
# zip happened to be there) prompted this — daily zips give us a known
# floor on data-recovery RPO. Pruner CronJob (pb-backup-prune below)
# handles rotation.
#
# Uses alpine/curl (curl + jq) so we can extract the auth token cleanly
# from the JSON response without sed gymnastics. Hits PB cluster-internal
# via the homelab.svc.cluster.local Service.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pb-backup-daily
  namespace: homelab
spec:
  schedule: "0 9 * * *"  # 2 AM Pacific (PDT = UTC-7); slips by an hour during PST
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: backup
              image: alpine/curl:latest
              command: ["sh", "-c"]
              args:
                - |
                  set -e
                  # alpine/curl ships only curl — install jq for token parsing.
                  apk add --no-cache jq >/dev/null
                  PB=http://pocketbase.homelab.svc.cluster.local:8090
                  # Superuser auth — PB returns 200 + {token: "..."} on success.
                  # NOTE: PB session tokens go in `Authorization: <token>` (no
                  # Bearer prefix), unlike hlk_/mcpat_ tokens which DO use Bearer.
                  TOKEN=$(curl -sf -X POST "$PB/api/collections/_superusers/auth-with-password" \
                    -H "Content-Type: application/json" \
                    -d "{\"identity\":\"$PB_ADMIN_EMAIL\",\"password\":\"$PB_ADMIN_PASSWORD\"}" \
                    | jq -er .token)
                  # PB enforces lowercase-only on backup names (regex
                  # `^[a-z0-9_-]+\.zip$`), so use lowercase `t`/`z`
                  # separators instead of the ISO 8601 uppercase form.
                  KEY="daily-$(date -u +%Y-%m-%dt%H-%M-%Sz).zip"
                  curl -sf -X POST "$PB/api/backups" \
                    -H "Authorization: $TOKEN" \
                    -H "Content-Type: application/json" \
                    -d "{\"name\":\"$KEY\"}"
                  echo "[pb-backup-daily] created $KEY"
              envFrom:
                - secretRef:
                    name: api-secrets
---
# Backup pruner — runs just after the daily backup. Tiered retention:
#
#   - daily-*           : keep 90 days, EXCEPT the chronologically-first
#                         daily-* of each calendar month, which is exempted
#                         from pruning and kept forever (virtual "monthly"
#                         tier — no separate file naming, no separate cron).
#   - pre-deploy-*      : keep 14 days
#   - pre-migration-*   : keep forever (manual / migration-related, rare,
#                         load-bearing — the only thing that saved the
#                         2026-05-22 life_events incident)
#   - anything else     : keep forever (be conservative — manual backups
#                         the user took get the same protection)
#
# Sizing note: at ~30 MB/snapshot, 90 daily + ~12 monthlies/year ≈ 3 GB/yr.
# Disk on the VPS has ~13 GB free as of 2026-05-22 — plenty of headroom.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pb-backup-prune
  namespace: homelab
spec:
  schedule: "5 9 * * *"  # 5 min after pb-backup-daily
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: prune
              image: alpine/curl:latest
              command: ["sh", "-c"]
              args:
                - |
                  set -e
                  # alpine/curl ships only curl — install jq for token + filtering.
                  apk add --no-cache jq >/dev/null
                  PB=http://pocketbase.homelab.svc.cluster.local:8090
                  TOKEN=$(curl -sf -X POST "$PB/api/collections/_superusers/auth-with-password" \
                    -H "Content-Type: application/json" \
                    -d "{\"identity\":\"$PB_ADMIN_EMAIL\",\"password\":\"$PB_ADMIN_PASSWORD\"}" \
                    | jq -er .token)
                  NOW=$(date -u +%s)
                  DAILY_CUTOFF=$((NOW - 90*86400))
                  PREDEPLOY_CUTOFF=$((NOW - 14*86400))
                  # GET /api/backups returns [{key, size, modified}, ...].
                  # `modified` is RFC3339-ish UTC ("2026-05-22 09:00:00.123Z"
                  # in current PB versions). Normalize space→T and strip
                  # sub-seconds so busybox date can parse it.
                  BACKUPS=$(curl -sf "$PB/api/backups" -H "Authorization: $TOKEN")
                  # Monthly exempt set: chronologically-first daily-* of each
                  # calendar month (grouped by YYYY-MM extracted from key).
                  # Lexicographic min_by(.modified) === chronological min for
                  # the RFC3339-ish string PB returns. Written to a file so
                  # the while-loop subshell below can grep it cheaply.
                  MONTHLY=$(printf '%s' "$BACKUPS" | jq -r '
                    [.[] | select(.key | test("^daily-"))]
                    | group_by(.key | capture("^daily-(?<ym>[0-9]{4}-[0-9]{2})") | .ym)
                    | map(min_by(.modified) | .key)
                    | .[]
                  ')
                  echo "[pb-backup-prune] monthly exempts:"
                  printf '  %s\n' $MONTHLY
                  printf '%s' "$BACKUPS" \
                    | jq -r '.[] | "\(.key)\t\(.modified)"' \
                    | while IFS="$(printf '\t')" read -r KEY MODIFIED; do
                        ISO=$(printf '%s' "$MODIFIED" | sed 's/ /T/; s/\.[0-9]*Z/Z/; s/\.[0-9]*$//')
                        TS=$(date -u -d "$ISO" +%s 2>/dev/null || echo "")
                        if [ -z "$TS" ]; then
                          echo "[pb-backup-prune] skip (unparseable date) $KEY $MODIFIED" >&2
                          continue
                        fi
                        case "$KEY" in
                          daily-*)
                            if printf '%s\n' "$MONTHLY" | grep -qxF "$KEY"; then
                              # Monthly exempt — keep forever.
                              continue
                            fi
                            if [ "$TS" -lt "$DAILY_CUTOFF" ]; then
                              echo "[pb-backup-prune] delete (>90d) $KEY"
                              curl -sf -X DELETE "$PB/api/backups/$KEY" -H "Authorization: $TOKEN"
                            fi
                            ;;
                          pre-deploy-*)
                            if [ "$TS" -lt "$PREDEPLOY_CUTOFF" ]; then
                              echo "[pb-backup-prune] delete (>14d) $KEY"
                              curl -sf -X DELETE "$PB/api/backups/$KEY" -H "Authorization: $TOKEN"
                            fi
                            ;;
                          *)
                            # pre-migration-* and any manually-named backups — keep.
                            ;;
                        esac
                      done
                  echo "[pb-backup-prune] done"
              envFrom:
                - secretRef:
                    name: api-secrets
