apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pocketbase-data
  namespace: homelab
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pocketbase
  namespace: homelab
spec:
  serviceName: pocketbase
  replicas: 1
  selector:
    matchLabels:
      app: pocketbase
  template:
    metadata:
      labels:
        app: pocketbase
    spec:
      containers:
        - name: pocketbase
          image: localhost:30500/homelab/pocketbase:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8090
          volumeMounts:
            - name: data
              mountPath: /pb/pb_data
          resources:
            requests:
              memory: 128Mi
              cpu: 50m
            limits:
              # Bumped from 256Mi → 1Gi after 2026-05-21 OOMKill during a
              # post-deploy reconnect storm. Single-tenant VPS has plenty of
              # headroom; the previous limit was conservative and started
              # biting now that more clients + the api service connect at once.
              memory: 1Gi
              cpu: 500m
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pocketbase-data
---
apiVersion: v1
kind: Service
metadata:
  name: pocketbase
  namespace: homelab
spec:
  selector:
    app: pocketbase
  ports:
    - port: 8090
      targetPort: 8090
