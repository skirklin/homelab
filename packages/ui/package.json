{
  "name": "@homelab/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared React components (placeholder)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'placeholder — no build yet'"
  }
}
