rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Grocery lists - require authentication only (personal app)
    match /lists/{listId} {
      allow read, write: if request.auth != null;

      match /items/{itemId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
